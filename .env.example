# LLM provider keys (passed to LiteLLM via docker-compose)
OPENROUTER_API_KEY=

# Tool keys
GITHUB_PAT_TOKEN=

# To use local Ollama models, set LLM_*_MODEL vars to ollama/<model-name>
# See README.md "Local LLM (Ollama)" section for details.
