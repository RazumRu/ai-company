{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": ["tsconfig.json", "tsconfig.build.json"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**"],
      "outputs": ["dist/**"]
    },
    "build:tests": {
      "dependsOn": ["^build"],
      "inputs": ["src/**"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "outputs": [],
      "inputs": ["src/**"]
    },
    "lint:fix": {
      "outputs": [],
      "inputs": ["src/**"]
    },
    "test": {
      "inputs": ["src/**"]
    },
    "test:unit": {
      "inputs": ["src/**"]
    },
    "test:integration": {
      "inputs": ["src/**"],
      "env": [
        "INTEGRATION_USE_LOCAL_DEPS",
        "INTEGRATION_WORKER_COUNT",
        "POSTGRES_URL",
        "REDIS_URL",
        "QDRANT_URL"
      ]
    },
    "test:cov": {
      "inputs": ["src/**"]
    },
    "test:e2e": {
    },
    "test:e2e:local": {},
    "start:dev": {}
  }
}
